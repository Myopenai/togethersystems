# T,. OSOTOSOS Build Output Location

**LOGO:** `T,.&T,,.&T,,,.(C)TEL1.NL`

## 📂 Build-Verzeichnis

### Haupt-Verzeichnis

**Relativer Pfad:**
```
builds/go-executable/build/
```

**Vollständiger Pfad:**
```
D:\busineshuboffline CHATGTP\TOGETHERSYSTEMS- GITHUB\Nieuwe map (3)\builds\go-executable\build\
```

---

## 📦 Build-Struktur

Nach dem Build findest du die Binaries in:

```
build/
├── windows-amd64/
│   └── ostosos-server.exe
├── windows-arm64/
│   └── ostosos-server.exe
├── macos-amd64/
│   └── ostosos-server
├── macos-arm64/
│   └── ostosos-server
├── linux-amd64/
│   └── ostosos-server
├── linux-arm64/
│   └── ostosos-server
└── linux-386/
    └── ostosos-server
```

---

## 🔍 Builds finden

### Windows Binary:
```
builds/go-executable/build/windows-amd64/ostosos-server.exe
```

### Linux Binary:
```
builds/go-executable/build/linux-amd64/ostosos-server
```

### macOS Binary:
```
builds/go-executable/build/macos-arm64/ostosos-server
```

---

## ✅ Prüfen ob Builds existieren

### PowerShell:
```powershell
cd "D:\busineshuboffline CHATGTP\TOGETHERSYSTEMS- GITHUB\Nieuwe map (3)\builds\go-executable"
dir build\ -Recurse
```

### Explorer:
1. Öffne Explorer
2. Navigiere zu: `builds\go-executable\build\`
3. Du siehst alle Build-Verzeichnisse

---

## 🚀 Installer-Verzeichnis

Die Installer werden gespeichert in:

**Relativer Pfad:**
```
builds/go-executable/installers/
```

**Vollständiger Pfad:**
```
D:\busineshuboffline CHATGTP\TOGETHERSYSTEMS- GITHUB\Nieuwe map (3)\builds\go-executable\installers\
```

Struktur:
```
installers/
├── windows/
│   └── ostosos-server-1.0.0-windows.zip
├── macos/
│   └── ostosos-server-1.0.0-macos.dmg
└── linux/
    └── ostosos-server_1.0.0_amd64.deb
```

---

## 📝 Zusammenfassung

| Typ | Verzeichnis |
|-----|-------------|
| **Builds** | `builds/go-executable/build/` |
| **Installer** | `builds/go-executable/installers/` |
| **Scripts** | `builds/go-executable/` |

---

**T,.&T,,.&T,,,.T.** - Together Systems, Startup Systems

