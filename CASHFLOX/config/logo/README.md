Place your header logo PNG here and set its path in `config/fabrikage_pdf.config.json`:

- Expected path (default): `config/logo/fabrikage_header.png`
- Recommended size: ~120×20 px (it will be scaled proportionally)
- Transparent PNG preferred

If no image is present, a text fallback “FABRIKAGE” will be drawn.


